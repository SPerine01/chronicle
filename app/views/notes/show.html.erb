<h1> <%= @note.title %> </h1>

<p> <%= @note.content %> </p>

<%= link_to "My Notes", notes_path %>

<%= link_to "Edit Notes", edit_note_path(@note) %>
<%= link_to "Delete", note_path(@note), method: :delete, data: { confirm: "This action is permanent. Are you sure?" } %>